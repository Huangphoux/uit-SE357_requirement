= Update System Settings

#table(
  columns: (auto, 1fr),
  [*Epic*], [System Administration],
  [*Actor*], [Admin],
  [*MVP*], [No],
  [*BR*], [BR-SYS-5],
  [*Description / Notes*], [Update various system-wide settings.],
  [*As a*], [Admin],
  [*I want to*], [Update system settings],
  [*So that*], [I can configure the platform's behavior.],
  [*Trigger*], [The admin needs to modify a system setting.],
  [*Pre-condition*],
  [
    - Admin is logged in with system administrator privileges.
  ],

  [*Post-condition*],
  [
    - The system setting is updated.
  ],

  [*Solution (Step-by-Step)*],
  [
    - Go to the system settings page.
    - Modify the desired setting (e.g., site name, default language).
    - Save the changes.
  ],
)
